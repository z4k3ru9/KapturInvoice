# Repository instructions

Read [CLAUDE.md](CLAUDE.md) for the compact working contract and
[docs/README.md](docs/README.md) for task-specific references.

- Preserve existing changes; confirm the actual checkout before editing.
- Read applicable [.ai/rules](.ai/rules/index.md) before source changes.
- Use the locked dependencies already declared in `composer.json` and
  `package-lock.json`. Laravel Boost is already a dev dependency; do not
  run `composer require` or regenerate instructions as routine setup.
- Check PHP/Composer availability for application work; use the README setup
  when dependencies are absent. Documentation-only work requires no install.
- Keep financial and tenant boundaries intact. Update the relevant pending
  phase task with evidence; never infer a passing suite from old notes.
