# Gap analysis: Stitch Procurement / Delivery / Handover drafts vs. current implementation

## 0. Baseline facts (verified)

- **Nothing from Phase 05 exists in code.** `grep -rliE 'PurchaseOrder|VendorBill|DeliveryOrder|HandoverReport|ServiceReport|VendorPayment|JobType|job_type' app database/migrations database/factories tests routes` → zero hits. No `sales_orders.job_type` column either (`database/migrations/2026_09_14_090002_create_sales_orders_table.php`). Git HEAD (`9b7a009`) is docs-only after Phase 03; Phase 04 code is not started (no `sales_order_id` on `invoices`, no `docs/rebuild/outputs/18-*` checkpoint).
- Only procurement-adjacent code today: `app/Models/Vendor.php` (+`VendorContact`), `app/Models/Expense.php` (+`ExpenseTax`, `ExpenseCategory`), `app/Services/ExpenseTotalsCalculator.php`, `app/Filament/Resources/Vendors/**` (List/View pages, modal create/edit, `ContactsRelationManager`), `app/Filament/Resources/Expenses/**` (full pages + shared `app/Filament/RelationManagers/DocumentsRelationManager.php`), `app/Models/Document.php` (polymorphic `documents` table: disk/path/filename/mime_type/size — no uploader, no `documentable` validation of type/size).
- Both resources sit in nav group **`Expenses`** (`VendorResource.php:27`, `ExpenseResource.php:29`). DESIGN.md §2 requires **Procurement** (Vendors / Vendor Purchase Orders / Vendor Bills) and **Delivery** (Delivery Orders / Service Reports / Handover Reports). `AdminPanelProvider` defines no `navigationGroups()` ordering.
- `vendors` table has **no `tax_number`/NPWP** column; `clients.tax_number` exists (`ClientForm.php:38` is the pattern).
- `expenses` has no `number`, no status, no `due_date`, no paid amount, no job link; `tax_rates.rate` is `decimal(8,3)` (signed) and `ExpenseTotalsCalculator::syncTaxes()` sums every rate additively — i.e. no notion of withholding.
- `DocumentsRelationManager` `FileUpload::make('path')->directory('documents')` has **no** `acceptedFileTypes()`/`maxSize()`; FINALIZED-DECISIONS §5 mandates PDF/JPG/JPEG/PNG, ≤10 MB, uploader+timestamp recorded, private storage.
- Filament is **v5.7.8** (`composer.lock`).
- Stitch drafts were generated from `kapturinvoice-stitch-prompts.md` (job-workspace tab prompts §5-7) plus a Stitch-side `compliance-audit-addendum.md` that **is not an approved project doc** — it invents DJP e-Bupot sync, BSrE SHA-256 seals, GPS geofence, e-Materai, WAPU, 14-day portal tokens, "multi-party dual approval". memory.md §"Binding product decisions": not a certified tax system, tax output is a bookkeeping aid; deferred = vendor login, client uploads, e-signing, inventory, gateway. Treat every addendum-derived element as [STRIP] unless the Phase 05 spec names it.

Cross-cutting [STRIP] tells present in **all eight** drafts (DESIGN.md §16 "generic tells" + scope): all-caps eyebrow labels ("FISCAL Y2026 / Q3 AUDIT READY"), middle-dot meta strings, "DJP Live / e-Faktur Sync: Operational / Manifest Hash" status chips in the top bar, "System Online v2.4" footer, KPI tile rows with sparkline deltas, SHA-256/e-Materai/BSrE seal badges, "Session Signature"/"Audit Status: Compliant with UU…" footers, NPWP-validity badges ("PKP Valid", "DJP Active Taxpayer").

---

## 1. `vendors-vendor-bills-register-axen.html` — Vendors & Vendor Bills register

**Stitch layout.** H1 "Vendors & Accounts Payable"; header actions Export CSV / + New Vendor Bill / + New Vendor. 4 KPI tiles (Active Suppliers, Outstanding AP, Total Spend FY, PPh 23 Withholding Pool). Three sub-tabs: Vendor Bills (AP Ledger), Supplier Directory, DJP Tax Withholding. Filters: search (vendor/bill #/NPWP/ref), payment status (Unpaid/Paid/Partially Paid/Overdue), tax mode (PPh 23 2% / PPh 4(2) / PPh 21 / Non-withholding), fiscal period. Table columns: Bill # & Date | Vendor & Tax ID (NPWP + "PKP Valid") | Job Allocation / PO Ref (SO number, job title, PO number) | Tax Breakdown (subtotal, PPh 23 −2%, net, "PPN 11% included") | Total Payable | Due Date & Aging ("Due in 25d"/"Settled") | Status (Approved for Payment / Awaiting Approval / Paid) | Actions (icon trio). Footer banner: DJP e-Bupot compliance blurb.

**Current implementation.** `VendorsTable.php` (name/email/phone/created_at, TrashedFilter, View/Edit), `VendorForm.php`, `VendorInfolist.php`, `ViewVendor.php` + `ContactsRelationManager`. Vendor bills: **none** — closest is `ExpensesTable.php` (vendor/category/date/amount/total/rebill/client).

**Gaps.**
- [FIX-NOW] Move `VendorResource` (and `ExpenseResource`, `ExpenseCategoryResource`) out of nav group `Expenses` into `Procurement` per DESIGN.md §2: edit `$navigationGroup = 'Procurement'` in `app/Filament/Resources/Vendors/VendorResource.php:27` and `app/Filament/Resources/Expenses/ExpenseResource.php:29` (check `app/Filament/Resources/ExpenseCategories/*Resource.php` too); add `->navigationGroups(['Sales','Billing','Procurement','Delivery','Catalog','Clients','Settings',...])` ordering in `app/Providers/Filament/AdminPanelProvider.php` so Procurement precedes Catalog. Update `docs/filament-admin-layout-design.md` §2.4 table row accordingly (not a code change; flag for the implementer).
- [FIX-NOW] Vendor tax id: add nullable `tax_number` (string) to `vendors` via `php artisan make:migration add_tax_number_to_vendors_table`, add to `#[Fillable]` in `app/Models/Vendor.php`, add `TextInput::make('tax_number')->label('Tax number (NPWP)')` in `VendorForm.php` Section 'Vendor', `TextEntry::make('tax_number')` in `VendorInfolist.php`, and `TextColumn::make('tax_number')->searchable()->toggleable()` in `VendorsTable.php`. Plain string — no format validation, no "PKP Valid" badge ([STRIP], DJP validation is out of scope).
- [FIX-NOW] Vendor table: add `TextColumn::make('expenses_count')->counts('expenses')->label('Expenses')` and `TextColumn::make('expenses_sum_total')->sum('expenses','total')->money('IDR')` so the directory shows spend without a KPI tile row; add `Filament\Tables\Filters\SelectFilter::make('country_code')` only if useful. Add the standard `ActionGroup` row menu (View primary, Edit in menu) per DESIGN.md §8 "one safe primary row action plus a consistent row menu".
- [FIX-NOW] Expenses table (interim "vendor bill-like" list): add `SelectFilter::make('vendor_id')->relationship('vendor','name')`, `SelectFilter::make('expense_category_id')->relationship('category','name')`, a `Filter` with `DatePicker` from/to on `expense_date`, `->money('IDR')` on `subtotal`/`total`, `->defaultSort('expense_date','desc')`, and `TextColumn::make('documents_count')->counts('documents')->label('Evidence')`. Do **not** add status/paid/due columns — those columns don't exist and inventing them on `expenses` pre-empts Phase 05's `vendor_bills`.
- [NEW-PHASE] Vendor Bill register itself (Specs §05 "Vendor bills flow through Draft, Submitted, Approved, Partially Paid, Paid"; data model `vendor_bills`/`vendor_bill_items`/`vendor_payments`/`vendor_payment_allocations`). Minimum fields the draft implies: `number` (new `DocumentNumberGenerator` sequence, e.g. `VBL`), `vendor_id`, `vendor_purchase_order_id` (nullable), `vendor_invoice_reference` (their Faktur/invoice no.), `issue_date`, `due_date`, `terms`, `status`, `subtotal`, `tax_total`, `total` (gross), `paid_total` (derived from verified vendor payment allocations), `currency_code`, items (description, qty, unit, unit_cost, tax), documents. Status badge mapping in the draft (Awaiting Approval / Approved for Payment / Paid) ≈ spec's Submitted / Approved / Paid — use spec names. "Job Allocation" column should read from `job_cost_allocations`, showing "Shared (n)" when >1 job and "Unallocated" when remainder > 0 (Specs: "Show unallocated remainder").
- [STRIP] PPh 23 automation: "Tax Mode" filter, "PPh 23 Withholding Pool" tile, "DJP Tax Withholding" sub-tab, "DJP e-Bupot synchronized" footer, NPWP validation badges. Spec has no withholding model; memory.md: tax output is a bookkeeping aid. At most, Phase 05 may store a plain `withholding_amount` on a vendor bill/payment as a recorded figure — requires change control, not a UI invention.
- [STRIP] KPI tile row (4 tiles) and "Export CSV" as first-class header button; DESIGN.md §8 exports are a table toolbar concern (`Filament\Actions\ExportAction` when wanted), not page chrome.
- Omission vs spec: draft has no "Partially Paid" status row and no paid-amount column although spec requires partial vendor payments; no evidence indicator column (spec: "Vendor bills support partial vendor payments and evidence").

---

## 2. `vendor-purchase-orders-register-axen.html` — Vendor PO register

**Stitch layout.** H1 "Vendor Purchase Orders"; Export CSV / + New Vendor PO. 4 KPI tiles (Active POs, Committed Spend, Pending Delivery, Outstanding Bills). Filters: search, vendor select, job select (incl. "AXN-INV-INTERNAL (Buffer Stock)"), quarter. Status tabs with counts: All / Draft / Sent-Awaiting Confirmation / In Transit / Received. Table: PO Number & Date | Vendor Organization (name, NPWP, email) | Job Allocation & Scope (SO, description, "Single Job"/"Shared Allocation (2)"/"Internal Stock") | Delivery ETA & Courier (date, carrier, GRN #) | PO Total (with "Excl. PPh 23"/"Inc. 11% PPN" sub-label) | Status (In Transit / Confirmed / Received / Draft / Sent-Pending) | Actions. Footer: "Statutory Tax Reconciliation Note".

**Current implementation.** None.

**Gaps.**
- [NEW-PHASE] Entire screen (Specs §05 "Vendor PO stores items, quantity, cost, terms, due date, delivery date, notes, and attachments"; IMPLEMENTATION-STRUCTURE §Procurement; data model `vendor_purchase_orders`/`_items`). Minimum fields implied: `number` (new sequence, e.g. `VPO`), `vendor_id`, `status` (spec/prompt §5 says **Draft/Sent/Confirmed/Received** + Cancelled — the draft's extra "In Transit" is not in the prompt; reconcile: either drop it or make it a sub-state of Sent, decided at spec time), `issue_date`, `expected_delivery_date`, `due_date`, `terms`, `subtotal`/`tax_total`/`total`, `notes`, items, documents, and job links via `job_cost_allocations` (never a single `sales_order_id` — "One vendor purchase may serve multiple jobs"). Row actions from prompt §5: View, Receive, Cancel. Status tabs → `ListVendorPurchaseOrders::getTabs()` with `Tab::make()->modifyQueryUsing()` + `->badge()`.
- [NEW-PHASE] "Job Allocation" column should render Single/Shared(n)/Unallocated from allocations; **"Internal Stock" must not exist** — see [STRIP].
- [STRIP] "AXN-INV-INTERNAL (Buffer Stock)" job option and "Internal Stock" allocation label — inventory is deferred (memory.md); a PO line not allocated to a job is simply "Unallocated remainder" per Specs, never stock.
- [STRIP] Courier/GRN column ("DHL Express – Airfreight", "GRN #GRN-26-0412"): spec has no goods-receipt note or carrier tracking on the PO. Keep only `expected_delivery_date`; receipt is the `Received` status + evidence attachment.
- [STRIP] "Excl. PPh 23"/"Inc. 11% PPN" total sub-labels as tax-mode semantics (withholding out of scope); the PO total should show gross with net/tax components available in detail (Specs: "Use gross vendor cost for job margin while preserving net/tax/gross components").
- [STRIP] KPI tiles, "Live Sync" chip, statutory footer note, vendor email under NPWP in the vendor cell (DESIGN.md §16 restraint; vendor cell = name + optional tax number).

---

## 3. `vendor-po-creation-line-planning-axen.html` — New Vendor PO (line planning)

**Stitch layout.** Header: "Create Vendor Purchase Order", badges "Draft — Rev 01" / "Multi-Job Split Active"; Cancel / Save as Draft / Issue Vendor PO. §1 Vendor & Scope: vendor select (with NPWP, PKP status, "View Master Ledger"), Originating Primary Job select, PO Reference (auto-gen), Expected Delivery Date, Payment Terms select (Net 30 / Net 14 1.5% / Net 60 / 50-50 DP-BAST), Delivery Destination text (+warehouse code). §2 Line Items table: # | Item/SKU Description (asset class, HS code) | Qty | Unit | Unit Price | Subtotal (+PPN) | Tax Rate; blank trailing row with catalog SKU autocomplete; "Add Purchase Line", "Bulk Paste SKUs", "Import Lines from Source Quotation"; footer "Aggregated Gross Netto", "Total Weight". §3 Multi-Job Cost Allocation Matrix: Target Job | Client | Allocation % | Allocated Share | Impact State; "100% Balanced — Unallocated Remainder: Rp 0". Right rail: Cost & Tax Breakdown (DPP, PPN, PPh 23, Insurance, Net Payable, Gross), DJP Statutory Verification card, Job Margin Impact Covenant (allocated cost, contract billing, projected margin %, floor 40%, approval-level note); footer "Created By / Session Signature / Audit Status".

**Current implementation.** None. Closest patterns to reuse: quotation line editor (`app/Filament/Resources/Quotations/**` Items relation manager + `QuotationTotalsCalculator`), `SalesOrders/RelationManagers/MilestonesRelationManager.php` for "sum must equal" validation (`ApproveSalesOrder`), `DocumentNumberGenerator` for numbering.

**Gaps.**
- [NEW-PHASE] Full-width creation page (DESIGN.md §5 "Complex line editing uses a full-width page"). Minimum fields: as §2 above plus per-line `catalog_product_id` (nullable), `description`, `quantity`, `unit`, `unit_cost`, `tax_rate`/pricing mode consistent with `App\Enums\PricingMode` (one mode per document, DESIGN.md §7), `line_total`; `delivery_address` (free text); `terms` as free text or the company's existing terms convention — not the draft's hard-coded 4 options. Allocation section → `job_cost_allocations` rows: `sales_order_id`, `source_type/id` (PO line or bill line), `basis` (`quantity`|`amount` — Specs: "Allocate shared cost by explicit quantity or amount"; the draft's **percentage** input contradicts this → reconcile to qty/amount with a derived % display), `allocated_quantity`/`allocated_amount`, `actor_id`. Must show allocated total, unallocated remainder, each job's gross cost and margin impact before save, and reject allocation above the source line (DESIGN.md §7 Shared procurement; Specs "Prevent allocation above source line amount"). "Import Lines from Source Quotation" is a reasonable Phase 05 convenience (pull `sales_order_items` of the primary job as PO line suggestions) — spec-silent, flag for the implementer's judgment, not required.
- [NEW-PHASE] "Issue Vendor PO" = explicit `Draft → Sent` action via a review summary (DESIGN.md §7 Approval), never autosaved.
- [STRIP] Allocation by **percentage** column and "Impact State: Within Budget / Shared Cap Valid" badges; "Margin Impact Covenant" card with 40% floor and "approval level bypassed" copy (no margin-floor rule exists in specs; margin visibility is Owner/Admin-only per Phase 03 patterns, and the PO page should show margin impact only as the DESIGN §7 pre-save figure).
- [STRIP] PPh 23 / DPP / PPN "DJP Statutory Verification" rail, e-Bupot code, "Certificate Authority token", "Pre-validated" chips, "Total Weight", "HS Code"/"Asset Class" per line, "Warehouse Code / Gate pass" hints, "Bulk Paste SKUs", "View Master Ledger", "Fiscal Input Ledger: Buku Kas Besar", footer signatures. All beyond bookkeeping aid / inventory deferred.
- [STRIP] "Delivery Destination … Warehouse Bin A-14" — implies warehouse/inventory; keep a plain delivery address only.

---

## 4. `vendor-bill-detail-shared-allocation-axen.html` — Vendor Bill detail

**Stitch layout.** H1 "Vendor Bill BILL-2026-0082"; badges Approved for Payment / PPh 23 Applicable / Due in 25 days; meta "Target Settlement… • Disbursement Batch • Ledger Account 2100-AP-TRADE"; actions Edit Allocation / Download Vendor PDF / Record Settlement. 4 stat tiles (Gross total incl. PPN, Withholding −2% with BUPOT ref, Net Payable with bank, Job Cost Allocation 100% with SO-0001 75% / SO-0014 25%). "Vendor Entity & Ingress Metadata": vendor legal name + NPWP, originating PO, vendor tax invoice no. + e-Faktur no., issued/due/terms. Line items table: Item Description (SKU, serial range, "Asset Tagged & QC Passed") | Qty | Unit Price | Subtotal | Tax Rate; totals (lines, PPh 23 retention, payable); "Hardware Ingress Verification Log" (GRN, received qty, QC condition, inspector). "Multi-Job Cost Allocation Matrix": Job Reference (client) | Share % | Direct Cost | Margin Impact %; "Unallocated Remainder: Rp 0 — MATCHED". "Statutory Tax & Withholding Compliance" (Bukti Potong no., DPP, withheld, DJP validated timestamp). "Uploaded Evidence & Verification": 2 files with size and "Signed & Stamped"/"Verified by Warehouse Lead", drop-zone.

**Current implementation.** None for bills. `Expenses/Pages/ViewExpense.php` + `ExpenseInfolist.php` + `DocumentsRelationManager` is the only comparable detail page.

**Gaps.**
- [FIX-NOW] Expense view page hygiene that carries over to any future bill page: in `ExpenseInfolist.php` group entries into `Filament\Schemas\Components\Section`s (Vendor & dates / Amounts / Rebilling) with `->columns(2)` and `->money('IDR')` on `subtotal`/`tax_total`/`total`; move `legacy_expense_id`, `created_at`, `updated_at` into a collapsed "Record" section. Pure layout per DESIGN.md §1; no tests needed.
- [FIX-NOW] `app/Filament/RelationManagers/DocumentsRelationManager.php`: enforce FINALIZED-DECISIONS §5 on the shared evidence uploader — `FileUpload::make('path')->acceptedFileTypes(['application/pdf','image/jpeg','image/png'])->maxSize(10240)->disk('local')->visibility('private')->directory('documents')`; add `mime_type` badge column and `->openUrlInNewTab()` already present. Uploader/timestamp: `documents` has `created_at` but no `uploaded_by` — adding `uploaded_by_user_id` (nullable FK) via migration + setting it in the `CreateAction::using()` closure (`auth()->id()`) is a small, self-contained change; regression test: extend `tests/Feature/` (there is no existing documents-upload test — `tests/Feature/Documents/DocumentNumberingTest.php` is about numbering) with a Livewire relation-manager test asserting a `.exe`/12 MB upload is rejected and `uploaded_by_user_id` is set. Also add `DocumentsRelationManager` to `VendorResource::getRelations()` only if `Vendor` gains a `documents(): MorphMany` relation (one-line addition to `app/Models/Vendor.php`) — useful for vendor contracts/NPWP scans; optional.
- [NEW-PHASE] Vendor Bill detail (Specs §05; FINALIZED-DECISIONS §7 vendor payments as immutable events with proof upload before verification, one Vendor Payment Receipt per verified event, amendments/reversals). Layout to build: header (number, vendor, status badge, issue/due/terms, originating PO link, vendor's own invoice reference), lines table, totals (net / tax / gross), **allocation table** (Job | basis qty-or-amount | allocated amount | job gross cost | margin impact) with allocated total + unallocated remainder + over-allocation block, **vendor payments** list (date, amount, method, proof, verified/receipt link) with remaining due, evidence (`DocumentsRelationManager`), audit events. Actions: Submit / Approve (Accountant+ self-approve with audit evidence) / Record vendor payment (verify flow) / Download PDF. "Edit Allocation" must be blocked or amendment-linked once a payment is verified (Specs: allocations determine balances; post-receipt changes create a linked amendment). PO variance guard: payment above approved PO amount blocked until Admin/Owner approval with reason.
- [STRIP] Withholding tile/section (Bukti Potong, DJP validated timestamp, "PPh 23 Retention"), "Disbursement Batch", "Ledger Account 2100-AP-TRADE", "Mandiri Corporate Escrow VA", "Payment Authorization: Auto-Cleared" (there is no journal/GL — deferred), "Hardware Ingress Verification Log" (GRN/QC/inspector = inventory/goods-receipt, deferred), serial ranges/"Asset Tagged" on lines, "Signed & Stamped"/"Verified by …" evidence labels, "DESIGN.md §7 Cost Accounting Rule" eyebrow, "MATCHED" chip, allocation **%** as the input basis (use qty/amount, show % derived).
- Omission vs spec: no partial-payment history, no remaining-due figure, no Submit/Approve status transitions, no PO-variance warning, no unallocated state shown (draft only shows the 100% case).

---

## 5. `delivery-orders-handover-register-axen.html` — Delivery Orders & Handover register

**Stitch layout.** H1 "Delivery Orders & Handover Register"; actions Prepare BAST Report / + New Delivery Order. 4 KPI tiles (Total Dispatches, In Transit Today, Pending BAST Sign-off, Operational Handover Rate 94.2% +2.1%). Sub-tabs: Delivery Orders (Surat Jalan) 28 / Handover Reports (BAST) 19; toolbar Export Manifest / Batch Print SJ. Filters: search (DO #/BAST #/client/job/driver), status tabs All / In Transit / Delivered / Acknowledged-Signed. Table: DO Number & Date (+ "Batch 1 of 2"/"Single Batch"/"Inter-Island 3PL") | Client & Destination (address) | Job Reference | Items & Consignment (summary + serial range) | Carrier & Driver (fleet #, driver, plate, or airline AWB) | Status & Proof (badge + signed PDF link / ETA) | Actions (verify/track icon, print, menu). Below: "Recent BAST Certifications" cards (BAST no., title, date, signatory name+NIP, linked SO, SHA256 + e-Materai, Download PDF).

**Current implementation.** None. `SalesOrderStatus` has `Delivered`/`HandedOver` states driven by `TransitionSalesOrderStatus` with no evidence link.

**Gaps.**
- [NEW-PHASE] Delivery Order register (Specs §05 "Delivery Order applies to all applicable delivery events, including delivery-only jobs"; "Multiple partial Delivery Orders are allowed"; prompt §6 columns: DO number, Date, Items covered (count), Status Draft/Delivered/Acknowledged, evidence thumbnail, "View evidence"). Minimum fields: `number` (`DO` sequence), `sales_order_id` (required — always job-bound), `client_id` (denormalized from job), `delivery_date`, `delivery_address`, `status` (Draft → Delivered → Acknowledged; Cancelled from non-final — mirror the SVR pattern in FINALIZED §10 which says SVR "follows the Delivery Order pattern": Draft → Submitted → Approved, so the implementer must pick one naming for both and record it), `recipient_name` (acknowledgement name — plain text, no signature), `notes`, items (`sales_order_item_id`, description, quantity delivered), documents (proof photos/PDF). Optional, low-cost, spec-silent: `carrier_name`, `driver_name`, `vehicle_plate` as free text — acceptable only as plain fields, no fleet master.
- [NEW-PHASE] Handover sub-tab should be its own resource (see §7) and, per DESIGN.md §2, **Service Reports** must sit between them in the Delivery nav group — the draft omits Service Reports entirely (see "Omissions").
- [STRIP] "In Transit" status and ETA (no live tracking), "Batch 1 of 2 / Inter-Island 3PL / AWB / flight" logistics detail beyond free-text carrier, KPI tiles ("Operational Handover Rate +2.1%"), "Batch Print SJ"/"Export Manifest" toolbar (bulk issue/print is not a spec action; DESIGN.md §8 forbids bulk consequential actions), serial-number ranges in the items cell (inventory), "Recent BAST Certifications" cards with SHA256/e-Materai/NIP (e-signing deferred; use a plain "acknowledged by <name>" text and an uploaded scan).
- [STRIP] "Prepare BAST Report" as a register-level action: handover is created from the Job (prompt §6 split button "New Delivery Order / Record Handover" on the Delivery tab) and only when required; a global button invites creating handovers for goods jobs.

---

## 6. `delivery-order-detail-logistics-axen.html` — Delivery Order detail

**Stitch layout.** Breadcrumb; chips "DJP e-Faktur Sync: Operational", "Manifest Hash"; H1 "Delivery Order DO-AXN-2026-0412", badges Acknowledged/Signed + "Surat Jalan Fisik Verified"; actions Print Surat Jalan (A4) / Download Signed Proof / Generate BAST Draft. 4 tiles: Linked Job, Dispatched Items 4/4 (100%), Carrier & Fleet, Acknowledged At (timestamp + recipient). "Consignment Manifest & Serial Numbers" table: # | Item Description (SKU) | Serial Range | Disp. Qty | Recv. Qty | QC Inspection; barcode-scan note. "Ingress & Delivery Timeline" (Prepared/Dispatched/Arrived/Signed with timestamps + narrative). "Operational Handover Governance Checklist" (Physical Delivery / Warehouse Deduction / Serial Reconciliation). "Digital Proof & Signature" (signed PDF, stamp block "Penerima… RECEIVED", recipient name, designation, GPS lat/long, geo area). "Site Verification Photos" (2 geo-tagged photos). "Billing Progression: Milestone 2 (50%) Unlocked → Create Milestone 2 Invoice" + "View Linked Job".

**Current implementation.** None. Nearest structural analogue: `SalesOrders/Pages/ViewSalesOrder.php` + `SalesOrderInfolist.php`.

**Gaps.**
- [NEW-PHASE] DO detail (view page + infolist + items relation manager + `DocumentsRelationManager`), A4 PDF "Surat Jalan" via the existing dompdf pattern (`resources/views/pdf/*.blade.php`, `App\Filament\Support\DownloadPdfAction`), Bahasa default / English override (FINALIZED §10 says SVR shares the DO document treatment → DO is a launch document too, Phase 06B tests). Header actions: Mark delivered / Record acknowledgement (recipient name + date + proof upload) / Download PDF / "Open Job" deep link to the Delivery tab (DESIGN.md §4 "Deep links … preserve a link back"). Timeline = `AuditEvent` rows for this DO (`app/Models/AuditEvent.php`, `AuditLogger`), not hand-written narrative.
- [NEW-PHASE] "Generate BAST Draft" only when the job is installation/service (job type gate) — needs `sales_orders.job_type` (Specs: "Job type is goods, installation, or service, set on the job before approval"; add `App\Enums\JobType`, a required field before `ApproveSalesOrder`, and a regression in `tests/Feature/Sales/SalesOrderWorkflowTest.php`).
- [NEW-PHASE] "Billing Progression" card: keep only as a **link** to the Job's Billing tab ("Milestone n now eligible") — invoice creation from milestones is Phase 04 territory (`PaymentMilestone` exists, `payment_milestones` has no `invoice_id` yet). Do not put "Create Milestone Invoice" on the DO page.
- [STRIP] Serial ranges, QC inspection, "Recv. Qty" vs "Disp. Qty" reconciliation, barcode OS note, "Warehouse Deduction / Serial Reconciliation to PO" checklist (inventory deferred; delivery quantity is the only figure), GPS coordinates/geo area (no geolocation in spec), "Digital Proof & Signature" stamp/SHA-256 (e-signing deferred — an uploaded scanned proof + typed recipient name is the launch model), "Surat Jalan Fisik Verified" chip, manifest hash, e-Faktur chip, geo-tagged photo metadata (photos stay plain `documents`).

---

## 7. `handover-reports-register-axen.html` — Handover Reports register

**Stitch layout.** H1 "Handover Reports (BAST & BAUT)", "ISO 27001 Certified" badge; actions Export Audit Archive (.zip) / New Handover Report. Tiles: Total Handover Records, Pending Client Acceptance (value), "Goods-Only Waived Rate 42% — Annex B", BSrE Certified Seal Rate 100%. Filters: search, status tabs All / Pending Sign-off / Customer Accepted / Waived-Annex B, fiscal year select. Table: BAST # & Date | Client & Signatory (name, title) | Job & Contract Scope | Deliverable Scope Type ("Turnkey Hardware & Cabling SLA", "Goods-Only Supply – Waived") | Milestone Value | Inspection Status (Customer Accepted / Pending Inspection / Waived by Scope) | Digital Seal/BSrE | Actions (view, download / send). Footer "Corporate Governance & Revenue Recognition Rule §4: approved BAST unblocks Milestone Invoicing".

**Current implementation.** None.

**Gaps.**
- [NEW-PHASE] Handover Report resource (Specs §05: required only for installation/service jobs; may require completed delivery; Admin/Owner override with reason; service jobs additionally gated by an approved `Resolved` SVR and no open `Follow-up required`; `CompleteHandover` action in IMPLEMENTATION-STRUCTURE). Minimum fields: `number` (`HO`/`BAST` sequence — CONTEXT.md term is "Handover Report", Bahasa "Berita Acara Serah Terima"), `sales_order_id`, `handover_date`, `prepared_by_user_id`, `customer_acknowledgement_name` + optional title, `status` (Draft → Submitted/Pending acknowledgement → Acknowledged/Approved; Cancelled from non-final), `notes`/scope summary, `override_reason` (nullable, with audit event), documents (signed scan). Row actions: View, Download PDF, Send to billing contact (BillingMailer pattern, Phase 06). Status tabs: All / Pending / Acknowledged. **No "Waived" status** — a goods job simply has no handover (prompt §6: show "Not required for this job" muted state on the Job page), so the register should not list waived rows or a "Goods-Only Waived Rate".
- [STRIP] "BAUT", "ISO 27001 Certified", "BSrE Certified Seal Rate", "Digital Seal / SHA-256 / Pre-Signed Hash" column, "Export Audit Archive (.zip)", "Annex B" waiver concept, "Milestone Value" column (handover is evidence, not a billing document; the invoice unlock is a Job Billing-tab concern), "Deliverable Scope Type" taxonomy (use job type goods/installation/service), governance footer.
- Omission vs spec: no Service Report register/link; no override-with-reason state; no "delivery incomplete" blocking state; no distinction installation-vs-service gate.

---

## 8. `driver-field-logistics-mobile-signoff.html` — Mobile proof-of-delivery

**Stitch layout.** Mobile page "Proof Of Delivery": driver identity (name, fleet, van, plate), "Live GPS Lock"; DO reference + "BAST Handover" tag, SO ref, client, site address; Equipment Manifest list (item, serial, qty, check ticks, "All serial barcodes verified"); Recipient Information (name, designation, phone, "Geofence Validated" lat/long ±3.2m); **Recipient Signature** canvas ("Sign inside boundary with finger or stylus", "Legal BAST Binding", "e-Seal Ready"); On-Site Evidence photos (2/4, "Min. 2 required", watermarked); Pre-Transmission Verification checklist (3 items); primary "Complete Handover & Transmit BAST"; "Save Offline Draft"; "Thermal Receipt".

**Current implementation.** None. Filament panel is responsive; DESIGN.md §1: "tablet and phone support … delivery updates, document viewing, and proof uploads" — via the same responsive Filament pages, not a separate app.

**Gaps.**
- [NEW-PHASE] The legitimate core: a Staff-or-higher user on a phone opens the DO (Filament view page, responsive per DESIGN.md §8 "stacked record summaries, action menus"), runs "Record delivery/acknowledgement" (modal: `recipient_name`, optional `recipient_title`, `delivered_at`, `FileUpload` multiple images with `capture="environment"` — Filament v5 `FileUpload::make()->image()->multiple()->maxFiles(n)`; `->extraInputAttributes(['capture' => 'environment'])`), then the status action. No separate mobile route/layout is needed; QA it on phone width per DESIGN.md §13.
- [STRIP] Signature canvas ("Legal BAST Binding", "e-Seal Ready") — e-signing is deferred (FINALIZED §5 "No electronic signatures launch now… Customer signatures and portal signing are deferred"); replace with typed acknowledgement name + uploaded photo/scan of the signed paper Surat Jalan. GPS/geofence, "Live GPS Lock", serial barcode verification, "Min. 2 photos required" rule (spec has no photo minimum), "Save Offline Draft" (no offline mode in scope), "Thermal Receipt", "Transmit BAST" (a DO acknowledgement is not a Handover Report; conflating DO sign-off with BAST contradicts CONTEXT.md and the goods-only rule), driver/fleet/vehicle master data, phone/WhatsApp of recipient.
- [STRIP] Standalone "driver" persona: launch roles are Owner/Admin/Accountant/Sales/Staff/Auditor/Vendor entity/portal contact (memory.md); a driver is a Staff user or is not a user at all (evidence uploaded by office staff). Vendor login is deferred, so no vendor-driver access either.

---

## 9. Omissions in the Stitch set relative to Phase 05 spec (for the phase implementer)

1. **Service Report (`SVR`) has no screen at all** — no register, no detail, no nav item, no row on the Delivery register, no "Serviced" stage. Required by Specs §05 + FINALIZED §10: register (date, number, technician, result, status — "same row layout" as Delivery Orders per DESIGN.md §4), detail/create form (service date, technician user + optional external name, reported problem, diagnosis, action taken, parts used [evidence-only, no pricing], result enum, follow-up notes, customer acknowledgement name, evidence), Draft → Submitted → Approved (+Cancelled), approval snapshot, A4 PDF Bahasa/English, portal sharing like a DO. Nav: Delivery › Service Reports (DESIGN.md §2).
2. **Job type (goods/installation/service)** appears nowhere (draft's "Deliverable Scope Type"/"Waived" is a proxy). Needs `sales_orders.job_type`, set before approval, driving: handover requirement, `Delivered` vs `Serviced` stage label, SVR availability (goods jobs may not have SVRs), "Not required" muted state.
3. **Goods-only jobs must never look blocked by Handover** — drafts show "Waived" rows/badges instead of omission. Implement as absence + "Not required for this job" on the Job Delivery tab; register excludes them.
4. **Override-with-reason** (Admin/Owner) for handover without complete delivery / without Resolved SVR — no UI state in drafts; needs a confirmation modal with required reason + `AuditEvent`.
5. **Unallocated remainder as a first-class visible state** (Specs "Show unallocated remainder and exclude it from confidently allocated margin") — drafts only render the 100%-balanced case; PO/bill detail and Job Margin tab need the >0 remainder state and the over-allocation error state.
6. **Partial vendor payments + Vendor Payment Receipt** (FINALIZED §7) — no payment list, proof-before-verify flow, or receipt/amendment on the bill detail.
7. **PO variance block** (payment above approved PO until Admin/Owner approval with reason) — absent.
8. **Operational vs financial closure** shown as two independent indicators (data model + addendum agree) — DO/handover screens hint "Milestone unlocked" but never show closure state; belongs on the Job header (Phase 03 `operational_closed_at`/`financial_closed_at` already exist on `sales_orders`).
9. **Allocation basis** — drafts use %, spec requires explicit quantity or amount.
10. **Attachment rules** (PDF/JPG/JPEG/PNG, 10 MB, uploader + timestamp, private) — drafts show arbitrary "drop documents here"; enforce in the shared `DocumentsRelationManager` now (§4 [FIX-NOW]).

---

## 10. Consolidated [FIX-NOW] checklist (all against existing Vendors/Expenses/Documents code; Filament v5.7.8)

| # | Change | Files |
|---|---|---|
| 1 | Nav groups: `Expenses` → `Procurement`; add `->navigationGroups([...])` ordering | `app/Filament/Resources/Vendors/VendorResource.php:27`, `app/Filament/Resources/Expenses/ExpenseResource.php:29`, ExpenseCategories resource (verify path under `app/Filament/Resources/`), `app/Providers/Filament/AdminPanelProvider.php` |
| 2 | `vendors.tax_number` (migration + `#[Fillable]` + form/infolist/table column, searchable) | new migration, `app/Models/Vendor.php`, `app/Filament/Resources/Vendors/Schemas/VendorForm.php`, `.../Schemas/VendorInfolist.php`, `.../Tables/VendorsTable.php` |
| 3 | Vendors table: `expenses_count`/`expenses_sum_total` columns, `ActionGroup` row menu | `app/Filament/Resources/Vendors/Tables/VendorsTable.php` |
| 4 | Expenses table: vendor/category `SelectFilter`, date-range `Filter`, `money('IDR')`, `documents_count`, default sort | `app/Filament/Resources/Expenses/Tables/ExpensesTable.php` |
| 5 | Expense infolist sectioning + money formatting | `app/Filament/Resources/Expenses/Schemas/ExpenseInfolist.php` |
| 6 | Evidence rules on shared uploader: `acceptedFileTypes`, `maxSize(10240)`, private disk, `uploaded_by_user_id` (migration on `documents` + fill in `CreateAction::using()`), + feature test | `app/Filament/RelationManagers/DocumentsRelationManager.php`, `app/Models/Document.php`, new migration, new `tests/Feature/Documents/DocumentUploadRulesTest.php` |
| 7 | (Optional) `Vendor::documents()` MorphMany + `DocumentsRelationManager` on `VendorResource::getRelations()` | `app/Models/Vendor.php`, `app/Filament/Resources/Vendors/VendorResource.php` |

Run `vendor/bin/pint --dirty --format agent` and `php artisan test --compact tests/Feature/Documents tests/Feature/Sales` after. Do not add status/paid/due/job columns to `expenses` or any withholding logic — those belong to Phase 05's `vendor_bills`/`job_cost_allocations` and to change control respectively.
