# Stitch vs. implementation — Dashboard / App shell / First-run gap analysis

Scope: `dashboard-company-overview.html` (KA), `dashboard-company-overview-axen.html`, `first-run-zero-state-onboarding-axen.html`, the two logo SVGs, prompt §1, DESIGN.md §1–3/§9/§10/§16, `docs/filament-admin-layout-design.md` §1/§9. Read-only; nothing changed. Note: `vendor/` is **not installed** in this sandbox, so Filament API hints below are from the v4/v5 panel API (identical surface for the calls cited) — the implementing agent should `composer install` and confirm signatures in `vendor/filament/filament/src/Panel/Concerns/*` before coding.

---

## 0. Shared app shell (appears identically in all three Stitch screens)

### Stitch structure
- **Sidebar** `aside fixed w-64` on near-black `karunia-black #050708` (KA + Axen dashboards) / `inverse-surface #213145` (onboarding). Top: brand block (KA: red `receipt_long` glyph tile + "KapturInvoice / Enterprise Billing"; Axen: `<img alt="Axen Tech Indonesia">` logo + "Axen Tech / Indonesia Portal") + collapse button. Below: **company switcher** button (colored dot + "Karunia Abadi / Switch: Axen Tech ID") — onboarding instead uses a `<select>` in the topbar ("Axen Tech ID (HQ)", "Karunia Abadi PT").
- **Nav groups (uppercase 11–12px gray eyebrow labels)**: Main → Dashboard · Sales → Quotations, Sales Orders / Jobs, Customer Invoices, Payments & Receipts · Procurement → Vendors, Vendor Purchase Orders, Vendor Bills · Delivery → Delivery Orders, Handover Reports · Catalog → Products & Items · Reports → Financial Analytics · Settings → Company & Taxes. Active item: `bg-inverse-surface` + `border-l-[3px] border-karunia-red`. Sidebar footer: "System Online · v2.4" pill.
- **Topbar** `h-14 bg-surface/90 backdrop-blur`: search input "Search records, clients, invoices... (Ctrl+K)"; company pill ("Karunia Abadi" with red dot); "+ New" primary button; bell with red badge "3"; avatar + "Budi Santoso / Admin / Owner".
- **Identity rail**: no separate rail element — identity is carried by the 3px red left border on the active nav item, red dot in switcher/pill, `bg-primary` tiles.
- **Tokens (KA + Axen dashboards share one config — the Axen dashboard was NOT recolored, it still uses `primary #b81218`, `karunia-red #E63934`, pink-tinted surfaces `#fff8f7/#fff0ee/#ffe9e6/#ffe2de`, `on-surface #281716`; `axen-blue #5065A8` is defined but unused)**. Onboarding uses the real Axen palette: `primary #374d8e`, `primary-container #5065a8`, `surface #f8f9ff`, containers `#eff4ff/#e5eeff/#dce9ff`, `on-surface #0b1c30`, `inverse-surface #213145`. Status colors both: green `#10B981`, amber `#F59E0B`, red `#EF4444`, blue `#3B82F6`, gray `#6B7280`. Font Inter (Google Fonts) + Material Symbols Outlined. Radius scale: DEFAULT 2px, lg 4px, xl 8px, "full" 12px (i.e. max 8px on cards — matches DESIGN §1). `darkMode:"class"` declared, **zero `dark:` utilities used** — no dark variant rendered despite the prompt asking.
- Logos: `kapturinvoice-logo.svg` = red rounded square + chevron/arrow glyph + "KAPTUR." wordmark in white (for dark sidebar). `axen-logo.svg` = blue `#5065A8` square + "A" glyph + "Axen Tech / INDONESIA" in slate (`#0F172A`/`#64748B`, for light background).

### Current implementation
- `app/Providers/Filament/AdminPanelProvider.php`: `->colors(['primary' => Color::Amber])`, no `brandLogo`/`brandName`/`navigationGroups`/`viteTheme`/`renderHook`/`globalSearchKeyBindings`/`databaseNotifications`/`sidebarCollapsibleOnDesktop`; stock `Widgets\AccountWidget` still registered; tenancy on `Company` (slug), `RegisterCompany`/`EditCompanyProfile`.
- No Filament theme at all: only `resources/css/app.css` (Tailwind v4, Kinetic Obsidian tokens for the public homepage) — `vite.config.js` inputs are `app.css`/`app.js` only. No `resources/css/filament/`.
- Nav groups actually registered (from `navigationGroup` on resources/pages, alphabetical since no `->navigationGroups()`): Billing (Invoices, Recurring Invoices, Quotes[legacy], Credits, Payments), Catalog (Products, Tax Rates, Price List), Clients (Clients, Client Portal Invitations), Documents, Expenses (Expenses, Vendors, Expense Categories), Proposals (3), Sales (Quotations, Jobs), Settings (Branding, Client Portal, Email & Reminders, Invoice & Numbering, Payment Gateways), Team (Users). Projects/Task Statuses hidden. No `navigationSort` anywhere.
- Brand fields: `Company` fillable `logo_path`, `primary_color`, `secondary_color` (no casts); `Company::getLogoDataUri()`; editable in `app/Filament/Pages/Settings/EditBrandingSettings.php` (`FileUpload logo_path`, `ColorPicker primary_color/secondary_color`) and `EditCompanyProfile`. Seeder values: KA `#8c0d1b` (≠ DESIGN `#E63934`), Axen `#298AAB`/`#7081e0` (≠ DESIGN `#5065A8`). `docs/rebuild/DESIGN.md` §10 is the binding token source.
- Global search: no resource implements `getGloballySearchableAttributes()` (grep empty).
- Role: `User::companyRole(Company): ?CompanyRole`, `User::hasCompanyRole()`; `App\Enums\CompanyRole` = Owner/Admin/Accountant/Sales/Staff/Auditor.

### Gaps
| # | Gap | Tag |
|---|---|---|
| S1 | Sidebar nav groups/order/labels differ (Stitch = DESIGN §2 order: Sales, Procurement, Delivery, Catalog, Reports, Settings). Current shows legacy groups Billing/Clients/Expenses/Documents/Proposals/Team. | [FIX-NOW] partial — reorder/rename what exists; Procurement (Vendor PO/Bills), Delivery, Reports items are [NEW-PHASE] 05 / 06 |
| S2 | Primary color amber, not company identity red/blue; no per-tenant brand color. | [FIX-NOW] |
| S3 | No brand logo/name per company; sidebar not dark; no 3px identity accent. | [FIX-NOW] (logo/name); dark sidebar + accent via theme CSS |
| S4 | Seeded `primary_color` values contradict DESIGN §10 hexes. | [FIX-NOW] (seeder + memory note; no migration) |
| S5 | Global search with Ctrl+K placeholder. | [FIX-NOW] (`globalSearchKeyBindings` + `getGloballySearchableAttributes` on Client/Invoice/Quotation/SalesOrder) |
| S6 | Notification bell + count. | [FIX-NOW] shell only (`->databaseNotifications()` needs `notifications` table migration) — real notification *policies* are Phase 06 |
| S7 | Topbar "+ New" global create button. | [STRIP] — ambiguous action, not in DESIGN §2; keep per-resource Create |
| S8 | Sidebar footer "System Online · v2.4", "Enterprise Billing"/"Indonesia Portal" taglines, "Updated 2 mins ago", "Queue updates live on ERP webhook events". | [STRIP] — decorative/ fake status chrome (DESIGN §16) |
| S9 | Company switcher styled as sidebar card with color dot + "Switch: …" (and `<select>` in onboarding). | [STRIP] as-drawn — DESIGN §2: launch deployments are isolated per company; switching must be "deliberate + refresh". Keep Filament's stock tenant menu; never a color dot alone. |
| S10 | Material Symbols icons. | [STRIP] — DESIGN §1: existing icon system (Heroicons in Filament). Map: dashboard→`Heroicon::OutlinedHome`, request_quote→DocumentDuplicate (existing), assignment→Briefcase (existing), receipt→DocumentText, payments→Banknotes, storefront→BuildingStorefront, local_shipping→Truck, category→Cube, bar_chart→ChartBar, tune→Cog6Tooth. |
| S11 | Inter webfont from Google Fonts. | [FIX-NOW] optional — `->font('Inter')` is the Filament default (Google Fonts provider); app.css deliberately uses system stack for offline CI builds. Decide once; if adopted, also bundle for the theme file. Not blocking. |
| S12 | Legacy-only groups (Billing/Quotes legacy, Recurring Invoices, Credits, Proposals, Payment Gateways, Documents, Team, Expenses) visible. | [FIX-NOW] hide per memory.md "Routine Phase 04 judgments": Payment Gateways, Recurring Invoices, Credits, Proposals hidden from launch nav. Legacy Quotes/Invoices/Payments/Clients/Vendors/Expenses/Documents/Users stay until Phase 04/05 replace them — **do not delete resources**. |

### [FIX-NOW] execution steps (shell)
1. **Nav order** — in `AdminPanelProvider::panel()` add
   `->navigationGroups([ NavigationGroup::make('Sales'), NavigationGroup::make('Procurement'), NavigationGroup::make('Delivery'), NavigationGroup::make('Catalog'), NavigationGroup::make('Reports'), NavigationGroup::make('Settings'), NavigationGroup::make('Billing')->label('Billing (legacy)'), NavigationGroup::make('Clients'), NavigationGroup::make('Expenses'), NavigationGroup::make('Documents'), NavigationGroup::make('Team') ])` (`Filament\Navigation\NavigationGroup`; groups not listed render last). Dashboard has no group → renders at top (Stitch "Main"). Set `$navigationSort` on `QuotationResource` (10), `SalesOrderResource` (20), and set `SalesOrderResource::$navigationLabel = 'Sales Orders / Jobs'`? — **No**: memory/CLAUDE.md say nav label "Job"; keep `Jobs`, note Stitch label as [STRIP] wording. Move `VendorResource` (`app/Filament/Resources/Vendors/VendorResource.php:27`) `$navigationGroup` from `'Expenses'` to `'Procurement'` (Vendors are Phase 05 nav home per DESIGN §2). Add `shouldRegisterNavigation(): false` to `PaymentGatewayResource`, `RecurringInvoiceResource`, `CreditResource`, `ProposalResource`, `ProposalTemplateResource`, `ProposalSnippetResource` (pattern already in `ProjectResource.php:38`). Verify: `php artisan test tests/Feature/Filament` (there are per-resource render tests that call pages directly, so hiding nav is safe) + run app.
2. **Per-tenant primary color** — `->colors()` is evaluated before tenant identification, so don't put tenant logic there. Create `app/Http/Middleware/ApplyCompanyBrand.php` and register via `->tenantMiddleware([ApplyCompanyBrand::class], isPersistent: true)`; inside: `$c = Filament::getTenant(); FilamentColor::register(['primary' => Color::hex($c->primary_color ?: '#E63934')]);` (`Filament\Support\Facades\FilamentColor`, `Filament\Support\Colors\Color::hex`). Keep `->colors(['primary' => Color::hex('#E63934')])` as pre-tenant fallback (login page). Status colors stay Filament defaults `success/warning/danger/info/gray` — matches DESIGN §10 global palette; do not make them tenant-editable. **Needs running app to verify** (color CSS vars emitted in `<head>` after middleware).
3. **Seeder** — `database/seeders/CompanySeeder.php:43` → `'#E63934'`, add `'secondary_color' => '#050708'`; line 63/64 → `'#5065A8'` / keep secondary cool neutral (e.g. `#64748B`). Update `DashboardWidgetsTest` not needed (creates its own Company).
4. **Brand logo/name** — `->brandName(fn () => Filament::getTenant()?->name ?? 'KapturInvoice')` and `->brandLogo(fn () => Filament::getTenant()?->logo_path ? Storage::disk('local')->…` — `local` disk has no public URL (same reason `getLogoDataUri()` exists), so use `->brandLogo(fn () => ($c = Filament::getTenant()) && $c->getLogoDataUri() ? new HtmlString('<img src="'.$c->getLogoDataUri().'" alt="'.e($c->name).'" class="h-7">') : null)` + `->brandLogoHeight('1.75rem')`. Ship `kapturinvoice-logo.svg`/`axen-logo.svg` into `public/images/brand/` as fallbacks only if the Owner approves them as real logos (they're Stitch-generated placeholders — flag to user, don't assume). Browser/document title already = company via `getFilamentName()`.
5. **Theme CSS (dark sidebar, 3px accent, 8px radius, flat cards)** — run `php artisan make:filament-theme admin --pm=npm` (generates `resources/css/filament/admin/theme.css` + adds it to `vite.config.js` input, Tailwind v4 style with `@import '../../../../vendor/filament/filament/resources/css/theme.css'` and `@source` lines). Register `->viteTheme('resources/css/filament/admin/theme.css')`. In theme.css set: `.fi-sidebar{background:#050708}` (Axen: `#213145` — drive via `[data-company="…"]` attribute emitted by a `PanelsRenderHook::BODY_START`? Simpler: emit `<style>` from render hook `PanelsRenderHook::HEAD_END` with `--ki-shell:` and `--ki-accent:` from tenant `secondary_color`/`primary_color`, and reference `var(--ki-shell)` in theme.css), `.fi-sidebar-item-active .fi-sidebar-item-button{border-left:3px solid var(--ki-accent)}`, `--radius` caps at `0.5rem`, drop widget shadows (`.fi-wi-stats-overview-stat, .fi-section{box-shadow:none;border:1px solid …}`). Rebuild `npm run build` (CI already does). **Class names must be checked against installed Filament v5 CSS** (`vendor/filament/filament/resources/css/components/sidebar.css`) — v5 uses `fi-sidebar-*` but verify exact hooks.
6. **Global search** — `->globalSearchKeyBindings(['command+k', 'ctrl+k'])` + `->globalSearchFieldSuffix(fn () => 'Ctrl+K')`; add `protected static ?string $recordTitleAttribute = 'number'` + `getGloballySearchableAttributes(): ['number','client.name']` on `QuotationResource`, `SalesOrderResource`, `InvoiceResource`; `ClientResource` → `['name','email']`. Search must respect tenancy — `BelongsToCompany` global scope covers it; add a test that a Company-B record is not returned.
7. **Notifications bell** — `->databaseNotifications()` + `php artisan make:notifications-table && migrate`. Only the shell; senders are Phase 06 (reminder/notification policy).
8. **AccountWidget** — remove `Widgets\AccountWidget::class` from `->widgets([...])` (Stitch dashboard has no account card; user identity lives in topbar user menu).
9. `->sidebarCollapsibleOnDesktop()` (Stitch `menu_open` collapse button). `->darkMode()` default keeps system-follow; ensure no manual toggle is added (DESIGN §1 "no manual theme toggle at launch") — Filament's theme switcher lives in the user menu: disable via `->darkMode(true, isForced: false)`? Filament exposes user-menu theme switcher when dark mode on; DESIGN says system-controlled → **[STRIP] the switcher**: use `->darkMode(false)` is wrong (kills dark). Verify in v5 whether `->darkMode(hasDarkMode: true)` still shows the switcher; if so hide via theme CSS `.fi-theme-switcher{display:none}` and rely on `prefers-color-scheme`. Needs running app.

---

## 1. `dashboard-company-overview.html` (Karunia Abadi) & `-axen.html`

### Stitch layout summary
- **Page header**: breadcrumb "Portal › Overview"; `h1` "Dashboard"; "Active Role: Owner / Admin" chip; subtitle "Karunia Abadi operational billing & procurement oversight"; right cluster: "Updated 2 mins ago", **period dropdown** "This month ▾" (This week / This month / This quarter / This year / Custom range…), "Export summary", "Refresh".
- **Row 1 — 6 stat tiles** (`grid-cols-1 sm:2 lg:3 xl:6`, flat `bg-surface-container-lowest`, thin border, icon top-right, tabular numerals, delta pill): Total Revenue `Rp 1.482.500.000` ▲+12.4% · Outstanding Balance `Rp 348.200.000` ▲+4.1% · Overdue Invoices `5 invoices` "+2 overdue" (red) · Open Quotations `14 quotes` "+3 new" · Active Jobs `28 in progress` "92.4%" · Delivery & Handover `6 pending` "1 awaiting". Axen adds sub-values ("/ Rp 78.4M", "/ Rp 890M", "(2 today)") and captions "vs last mo." / "action req." / "this week" / "on schedule" / "sign".
- **Row 2 — chart card** "Revenue & Cash Inflow Trend", subtitle "Daily billed revenue vs collected payments for September 2026 (IDR)", legend Invoiced Revenue (red `#E63934`) / Cash Inflow (blue `#465b9d`), segmented toggle Combined / Gross Billed / Cash Collected, inline SVG area+line chart, Y axis Rp 0–500M, X 01–30 Sep, hover tooltip (date, "3 transactions", Billed/Collected). Below: link "View as accessible data table" toggling a table — Interval | Billed Invoices | Cash Inflow | Net Variance, 4 weekly rows (last "22–30 Sep (Proj.)").
- **Row 3 — two panels** (`lg:grid-cols-2`):
  - **Expiring Quotations**: header count pill "4 expiring soon", "View all quotes →". Rows: client name, number (brand color, tabular), status chip "Expiring in 2 days (14 Sep)" (amber) / "Expired yesterday (11 Sep)" (red, row tinted `error-container/20`), amount, button "Review" (or "Extend / Void" primary on expired row), `⋯` menu. Footer note "Quotations automatically expire at 23:59 WIB on validity date. Zone: UTC+7".
  - **Action Queue**: header count pill "8 items" (red), "Urgency: High to Normal", **role tabs** Owner/Admin (4) · Accountant (2) · Sales (1) · Auditor (1). Items (icon, bold title, tabular detail, link button): "3 payments awaiting verification / Total Rp 115.000.000 awaiting bank match → Verify in Finance"; "2 jobs blocked on milestone approval / Job KJA-SO-2026090001 & …04 → Review Milestones"; "5 customer invoices overdue (>14 days) / Total Rp 78.450.000 across 3 clients → View Overdue"; "1 vendor PO exceeding budget margin / PO-2026-0412 margin threshold alert (<18%) → Inspect PO". Footer "Queue updates live on ERP webhook events", button "Batch approvals (3)".
- **Skeleton section** (bottom, dev demo): "Async Loading Skeletons Standard" — 6 tile skeletons `p-3.5 rounded-xl bg-surface-container-low animate-pulse`, chart skeleton `h-28 … flex items-end` (bar-shaped placeholders), list-row skeletons.
- No empty states rendered. No dark variant. Axen file = same markup/colors, different copy (AXN numbers, `Q-AXN-…`), `arrow_forward` instead of labeled buttons in queue, no `⋯` menus, no "Batch approvals".

### Current implementation
- `app/Filament/Pages/Dashboard.php` — `HasFiltersForm` with `Select period` (`this_week/this_month/this_year/last_year/custom` from `App\Filament\Support\DashboardPeriod::PERIODS`) + custom `DatePicker`s. No quarter. Rendered as a form section above widgets, not a compact header dropdown.
- `app/Filament/Widgets/RevenueOverview.php` — 3 stats: Total revenue (completed `Payment.amount` in period), Pending invoices (count + balance), Overdue invoices (count + balance). No deltas. Currency via `number_format(…,2)` with `currency_code` prefix.
- `app/Filament/Widgets/RevenueTrendChart.php` — line chart, single "Revenue" dataset (completed payments per day/month), hard-coded amber `#f59e0b`. No table alternative, no second series.
- `app/Filament/Widgets/ExpiringQuotesWidget.php` — `TableWidget` over **legacy** `Invoice type=quote` (`due_date` ≤ +14d, Sent/Viewed, not converted), columns number/client/valid until (danger/warning color)/total/status, "View" → `QuoteResource`. Not over `Quotation.valid_until`.
- All three `$isLazy = false` (documented Filament placeholder bug — do **not** re-enable without retesting).
- Tests: `tests/Feature/Filament/DashboardWidgetsTest.php` (3 tests), `tests/Unit/DashboardPeriodTest.php`.

### Gaps
| # | Gap | Tag |
|---|---|---|
| D1 | Period control: add "This quarter"; render compact in page header, not a full form block. | [FIX-NOW] |
| D2 | Tiles: 6 vs 3. Add Open Quotations (`Quotation` status ∈ Approved/Sent), Active Jobs (`SalesOrder` status ∈ Approved/Procurement/InProgress/Delivered/HandedOver), rename "Pending invoices" → "Outstanding balance" (sum balance first, count second). | [FIX-NOW] |
| D3 | Tile "Delivery & Handover (pending / awaiting sign)". | [NEW-PHASE] 05-procurement-and-delivery |
| D4 | Trend delta vs previous period (▲/▼ %). | [FIX-NOW] for Total revenue only (compute same window shifted back by its length); tiles that are live snapshots (outstanding/overdue) have no meaningful "previous" — omit delta rather than fake it. |
| D5 | Chart: two series (Invoiced vs Collected). "Invoiced" requires canonical issued invoices with `document_date` — current legacy `invoices.total` by `invoice_date` is a usable interim. | [FIX-NOW] interim (legacy `Invoice type=invoice`, status ≠ Draft, by `invoice_date`); label "Invoiced (legacy)"; revisit in Phase 04. Brand color from tenant. |
| D6 | "View as table" accessible alternative + text summary (DESIGN §10 mandatory). | [FIX-NOW] |
| D7 | Chart segmented toggle Combined/Gross Billed/Cash Collected, tooltip "3 transactions", projected "(Proj.)" row. | [STRIP] — decoration; legend + tooltip suffice. Never show projected numbers on a financial dashboard. |
| D8 | Expiring Quotations should read canonical `Quotation.valid_until` (Sent, ≤ +7d amber / past red), link `QuotationResource::getUrl('view')`, empty state "No quotations expiring soon." | [FIX-NOW] — keep legacy quotes widget only if imported data still needs it; recommend replacing (legacy quotes are frozen imports; their expiry is not actionable). |
| D9 | "Extend / Void" button on expired row, `⋯` menus, "23:59 WIB" note. | [STRIP] — Expired is a terminal state (`QuotationStatus`); no "extend". Expiry runs via `ExpireQuotations` command; a footnote is fine but not required. |
| D10 | Action queue panel, role-aware. Items feasible now: overdue invoices (legacy `Invoice` status Sent/Viewed/Partial & `due_date < today` → link `InvoiceResource` index with `?tableFilters[status][value]=…` — no "overdue" filter exists; add a `Filter::make('overdue')` to `InvoicesTable.php:63`), jobs awaiting approval (`SalesOrder` Draft → `SalesOrderResource` index; add status `SelectFilter` to `SalesOrdersTable.php:40`), quotations awaiting decision (`Quotation` Sent → `QuotationsTable.php:45` add status filter), draft quotations (Sales). Role gating via `auth()->user()->companyRole(Filament::getTenant())`; Auditor → same list, non-interactive. | [FIX-NOW] (new `ActionQueueWidget`) |
| D11 | "payments awaiting verification", "bank match", "Verify in Finance". | [NEW-PHASE] 04-billing-and-receivables (verification, receipts). Do not map to `PaymentStatus::Pending` — legacy semantics differ. |
| D12 | "vendor PO exceeding budget margin", "Inspect PO", margin thresholds. | [NEW-PHASE] 05 |
| D13 | Role **tabs** letting one user view other roles' queues; "Batch approvals (3)"; "Urgency" label; "ERP webhook" footer. | [STRIP] — a user sees their own role's queue (DESIGN §3); batch approval contradicts per-record approval-with-reason rules. |
| D14 | "Export summary", "Refresh", "Updated 2 mins ago", breadcrumb "Portal › Overview", "Active Role" chip, subtitle. | [STRIP] — chrome; Filament already breadcrumbs. Role chip may be kept as plain text in page subheading only if useful for Auditor read-only clarity (optional). |
| D15 | Skeleton loading states. | [FIX-NOW] partial — Filament lazy widgets give skeletons, but lazy is off due to the documented placeholder bug. Do not toggle `$isLazy` globally; try re-enabling only on the new widgets whose `columnSpan` is a scalar (`1`/`'full'`), test render, revert if 500. |
| D16 | Dark variant. | [FIX-NOW] via theme tokens only (Filament dark mode already system-follow); nothing to copy from Stitch. |
| D17 | Header subtitle currency "Rp 1.482.500.000" formatting (id-ID thousands `.`). | [FIX-NOW] — current `number_format(…,2)` shows `IDR 1,482,500,000.00`; use `Number::currency($v, 'IDR', 'id')` or `number_format($v, 0, ',', '.')` with `Rp` prefix for IDR tenants; whole-Rupiah display aligns with the ceiling-rounding rule. |
| D18 | Financial Analytics page ("Reports" nav). | [NEW-PHASE] 06-documents-portal-reporting |

### [FIX-NOW] execution steps (dashboard)
1. **`DashboardPeriod`** (`app/Filament/Support/DashboardPeriod.php`): add `'this_quarter' => 'This quarter'` to `PERIODS` (order: week, month, quarter, year, last year, custom); `match` arm `now()->startOfQuarter()…endOfQuarter()`, `group_by 'day'` if ≤ 92 days else month (keep existing >60-day rule for consistency → quarter = month buckets; decide and pin in `tests/Unit/DashboardPeriodTest.php`). Add `previous(array $period): array` returning the same-length window immediately before (`start->subDays(diffInDays+1)`), used for deltas.
2. **`Dashboard.php`**: keep `HasFiltersForm`; make the form compact — `Select::make('period')->inlineLabel()->hiddenLabel()` inside `Filament\Schemas\Components\Grid::make(3)`; `getColumns(): 6` so tiles can span 1 each; override `getWidgets()` order: `RevenueOverview`, `RevenueTrendChart`, `ExpiringQuotationsWidget`, `ActionQueueWidget`. Optionally set `protected ?string $subheading` to tenant name.
3. **`RevenueOverview.php`**: return 5 `Stat`s — Total revenue (+ `->description('▲ 12.4% vs previous period')->descriptionIcon(Heroicon::ArrowTrendingUp)->color('success'|'danger')` computed from `DashboardPeriod::previous()`), Outstanding balance (sum `balance` of open invoices; description = count), Overdue invoices (count; description = amount), Open quotations (`Quotation::whereIn('status',[Approved,Sent])->count()`, description = sum `total` if column exists — check `Quotation` casts; else omit), Active jobs (`SalesOrder::whereIn('status',[Approved,Procurement,InProgress,Delivered,HandedOver])->count()`). Currency formatter: add `App\Support\Money::rupiah()` helper or use `Illuminate\Support\Number::currency(..., in: 'IDR', locale: 'id')` (needs `intl`; CI images have it — verify). Extend `DashboardWidgetsTest::test_revenue_overview_…` with Quotation/SalesOrder fixtures (factories exist? check `database/factories/` for `QuotationFactory`/`SalesOrderFactory` — if missing, use `Quotation::create` like the test already does for Invoice).
4. **`RevenueTrendChart.php`**: second dataset `Invoiced` from `Invoice::query()->where('type', InvoiceType::Invoice)->whereNotIn('status',[Draft])` summed on `invoice_date` (confirm column name in `app/Models/Invoice.php` casts) with the same bucket loop (refactor loop into one `bucket(callable $sumForRange)` to avoid duplicating). Colors: `borderColor` = `Filament::getTenant()->primary_color` for Collected/Revenue, `#465b9d`→ use Filament `info` gray-blue for Invoiced; set `options` `plugins.legend.display=true`. Chart type `'line'` with `fill:true` (area) is fine. Add `->description()`? — ChartWidget has `$description`; use it for the text summary ("Rp X collected across N days"). **Accessible table**: add `getFooter()`/blade view `resources/views/filament/widgets/revenue-trend-table.blade.php` rendering the same buckets in a `<details>` element titled "View as table"; ChartWidget supports `protected string $view` override only for the whole widget — simpler: a sibling `RevenueTrendTable` widget (`Filament\Widgets\Widget` with custom view, `columnSpan 'full'`, collapsed `<details>`), sharing the bucket computation via a new `App\Filament\Support\RevenueBuckets::forPeriod($period)` service (put the query logic there, test it in Unit).
5. **`ExpiringQuotationsWidget`** (new, `app/Filament/Widgets/ExpiringQuotationsWidget.php`, `TableWidget`, `columnSpan` 1 of 2 → set `Dashboard::getColumns()` accordingly or `columnSpan ['lg' => 3]` of 6): query `Quotation::where('status', QuotationStatus::Sent)->whereNotNull('valid_until')->where('valid_until','<=', now()->addDays(7))->orderBy('valid_until')`; columns `client.name`, `number`, `valid_until->date()->color(fn => isPast ? 'danger' : 'warning')->description(fn => "Expires in N days")`, `total`; `recordActions([Action::make('view')->url(fn ($r) => QuotationResource::getUrl('view', ['record'=>$r]))])`; `->emptyStateHeading('No quotations expiring soon.')`; `->paginated(false)`; `->heading('Expiring quotations')`; header action "View all" → `QuotationResource::getUrl('index')`. Decide fate of `ExpiringQuotesWidget` (legacy): remove from `getWidgets()` but keep class + its test only if imported legacy quotes must still surface; otherwise delete class + test case together. Add test modelled on `test_expiring_quotes_widget_…` using `Livewire::test(ExpiringQuotationsWidget::class)->assertSee($number)`.
6. **`ActionQueueWidget`** (new, `Filament\Widgets\Widget`, view `resources/views/filament/widgets/action-queue.blade.php`, `$isLazy=false`): `getViewData()` returns `items[]` of `{label, count, url, tone}` built by `App\Filament\Support\ActionQueue::for(User $user, Company $company): array` (testable, no Livewire). Role matrix now: Owner/Admin → overdue invoices, jobs Draft awaiting approval, quotations Sent awaiting customer decision, expired quotations to cancel; Accountant → overdue invoices (+ later Phase 04 verification); Sales → own Draft quotations (`created_by`? check `quotations` columns — if no owner column, show company-wide and note it), Sent awaiting decision; Staff → active jobs (delivery items are Phase 05); Auditor → same items rendered as plain text, `url=null` (no links/buttons). Use Filament blade components `<x-filament::section heading="Action queue">` + `<x-filament::link>`/`<x-filament::badge>`; **no nested cards**. Empty state text: "Nothing needs your attention." Links: `InvoiceResource::getUrl('index', ['tableFilters' => ['overdue' => ['isActive' => true]]])` after adding `Filter::make('overdue')->query(fn ($q) => $q->whereIn('status',[Sent,Viewed,Partial])->where('due_date','<',today()))` to `InvoicesTable.php`; `SalesOrderResource::getUrl('index', ['tableFilters' => ['status' => ['value' => 'draft']]])` after adding `SelectFilter::make('status')->options(SalesOrderStatus::class)` to `SalesOrdersTable.php`; same for `QuotationsTable.php`. Tests: `tests/Unit/ActionQueueTest.php` per role incl. Auditor has no URLs + company scoping (Company B records excluded).
7. **Skeletons**: after the above, try `protected static bool $isLazy = true` on `ActionQueueWidget` and `ExpiringQuotationsWidget` only; run `test_dashboard_page_renders` + open `/admin/karunia-abadi` in the browser; revert on 500 and leave the existing comment.
8. Update `docs/filament-admin-layout-design.md` §9 and `docs/testing-coverage.md` at the end of the slice (docs are part of the convention here; not new files).

---

## 2. `first-run-zero-state-onboarding-axen.html`

### Stitch layout summary
- Same shell (Axen palette, `inverse-surface #213145` sidebar, "KapturInvoice" + "ID" badge, `<select>` company switcher in topbar, "Ctrl+K" kbd, `sync_alt` + bell icons, user "Budi Santoso / Admin / Owner").
- `h1` "Workspace Setup & First-Run Guide"; lead "Welcome to Axen Technology Indonesia's Operational Billing Workspace. Your deployment is active with zero overhead transactions. Complete the guided p…"; header buttons "Import CSV Data", "+ Create First Quotation"; progress "1 of 4 completed (25%)"; status chips "DJP e-Faktur Engine: Validated", "PKP Status: PT Axen Technology".
- **4-step checklist** (`lg:grid-cols-4` cards, eyebrow "Phase 01…04"): 1 Legal Entity & Tax Registry (done — "NPWP: 01.829.412.8-013.000", "NSFP #004.24.89124000", "DJP NSFP range synced for FY 2025"); 2 Upload Catalog & Rate Cards ("Action Required", buttons "Add SKU", upload); 3 Add Client & Quotation ("Next Milestone", copy "Automated line-item tax calculation (PPN 11% & PPh 23) will apply instantly", button "Create Quotation"); 4 Bank Mandiri Escrow VA ("Pending Step 3", "Configure VA").
- **Zero-state hero** (`xl:grid-cols-3`, 2 cols): illustration, `h2` "No Active Jobs or Commercial Invoices", copy "…Once your first quotation is formally approved by the client, a dedicated Job Workspace will automatically material…", buttons "+ Create First Quotation", "Import Existing Clients / Products (CSV)".
- **Right column**: "Pre-Configured Templates / Quick Start" with two template cards ("Datacenter Colocation Project — 6 SKUs, PPN 11% Incl.", "Enterprise Network Overhaul — 14 SKUs, Hardware + Svc"); support card "Need assistance with Tax Setup? … Axen Corporate Finance support team in Jakarta HQ", link "Read Indonesian Compliance Guide →".
- Tokens: Axen palette above; `headline-xl 28px`, `tabular-mono` font-size; radius scale same.

### Current implementation
- No first-run / zero-state logic anywhere (`grep -ri onboard|first.?run|emptyState` in `app/` and `resources/views` → nothing). Filament tables show stock empty states ("No records"). `RegisterCompany` (`app/Filament/Pages/Tenancy/RegisterCompany.php`) collects name/slug/domain/currency/primary_color only. Tax settings model `CompanyTaxSetting` exists (Phase 01) — check fields before referencing.

### Gaps
| # | Gap | Tag |
|---|---|---|
| O1 | Dashboard zero-state: when the tenant has no `Quotation`/`SalesOrder`/`Invoice`, show a setup checklist instead of empty tiles. | [FIX-NOW] (small: a `SetupChecklistWidget` shown only when counts are 0; steps = Company profile & numbering complete (`Company::code`, `codes_locked_at`, tax setting row), ≥1 catalog item (`Product`), ≥1 client, first quotation). Each step links to the existing page/resource. Progress "n of 4". Empty state copy must "explain + one action" (DESIGN §9). |
| O2 | "Import CSV Data" / "Import Existing Clients / Products (CSV)". | [STRIP] — imports are `import:invoiceninja-v4/v5` CLI (docs/data-import.md) and Price List import (exists as "Import pricelist" header action). Link to Price List import instead of inventing a CSV wizard. |
| O3 | "DJP e-Faktur Engine: Validated", "NSFP range synced", "PKP Status", "NPWP" chips. | [STRIP] — FINALIZED-DECISIONS §9/memory: not a certified tax-compliance system; tax recap is a manual bookkeeping aid. Never claim DJP/e-Faktur integration or "validated". |
| O4 | "PPN 11% & PPh 23 will apply instantly", "PPN 11% Incl." badges. | [STRIP] — wrong tax rule: Axen = 12% PPN with 11/12 DPP Nilai Lain; no PPh 23 in scope; Karunia is non-tax. |
| O5 | "Bank Mandiri Escrow VA / Configure VA / auto-routing reconciliation". | [STRIP] — payment gateway/VA deferred (memory.md Deferred list). |
| O6 | "Pre-Configured Templates / Quick Start" project templates. | [STRIP] — no template feature in scope; "formal proposals" deferred; would require change control. |
| O7 | Support card "Axen Corporate Finance support team in Jakarta HQ", "Read Indonesian Compliance Guide →". | [STRIP] — fabricated org copy + arrow-appended link text (DESIGN §16). |
| O8 | Company `<select>` switcher in topbar. | [STRIP] (see S9). |
| O9 | Resource-level empty states with one useful action ("No quotations yet — Create quotation"). | [FIX-NOW] — `->emptyStateHeading()/->emptyStateDescription()/->emptyStateActions([CreateAction::make()])` on `QuotationsTable`, `SalesOrdersTable` ("Jobs are created from an accepted quotation" + link to Quotations), `ProductsTable`, `ClientsTable`. |

### [FIX-NOW] execution steps (first-run)
1. `App\Filament\Support\SetupChecklist::for(Company): array{steps: list<array{key,label,done,url}>, done: int, total: int}` — steps: `company_profile` (`filled($c->code) && filled($c->address_line_1)`) → `EditCompanyProfile::getUrl()`; `numbering` (`NumberingSequence` row exists or `EditNumberingSettings`) ; `catalog` (`Product::query()->exists()`) → `ProductResource::getUrl('index')`; `client` → `ClientResource`; `quotation` → `QuotationResource::getUrl('create')`. Unit-test it (tenant-scoped).
2. `SetupChecklistWidget` (`Filament\Widgets\Widget`, view `resources/views/filament/widgets/setup-checklist.blade.php`, `columnSpan 'full'`, `$isLazy=false`, `canView(): !SetupChecklist::isComplete(Filament::getTenant())`). Put first in `Dashboard::getWidgets()`. Flat `<x-filament::section>` with a 4-column grid on `lg`, one column on phone; each step = check icon + label + `<x-filament::link>`; progress text "1 of 4 completed". No "Phase 0x" eyebrows, no "Action Required" pills — use the global status colors (green done / gray pending) with icon + text.
3. When `SetupChecklist` incomplete **and** zero quotations, `RevenueOverview`/chart still render (real zeros) — acceptable; do not hide them (avoids two dashboards). Stitch's hero "No Active Jobs…" collapses into the checklist's quotation step.
4. Table empty states per O9 (pure `Filament\Tables\Table` config; no tests needed beyond existing render tests).
5. Verify by `php artisan migrate:fresh --seed` + login → seeded companies have no quotations, so the checklist shows; then create one quotation and confirm it hides.

---

## Ordered implementation checklist for the next agent
1. Shell: S1 nav groups/hide deferred/Vendors→Procurement · S2 tenant color middleware · S3/S4 brand logo+name+seeder hexes · S5 global search · S8 remove AccountWidget · S9 collapsible sidebar. (`composer install` first; confirm Panel API names in `vendor/filament/filament/src/Panel/Concerns/`.)
2. Theme: `make:filament-theme admin`, `viteTheme`, shell CSS (dark sidebar, 3px accent, ≤8px radius, flat cards, hide theme switcher), `npm run build`.
3. Dashboard: D1 quarter+compact filter · D2/D4/D17 stats · D5/D6 chart + table · D8 quotations widget · D10 action queue + table filters · D15 lazy retry.
4. First-run: O1 checklist widget · O9 table empty states.
5. Tests: extend `tests/Feature/Filament/DashboardWidgetsTest.php`, `tests/Unit/DashboardPeriodTest.php`; add `ActionQueueTest`, `SetupChecklistTest`, global-search tenancy test. Run `php artisan test --compact tests/Feature/Filament tests/Unit`, `vendor/bin/pint --dirty --format agent`.
6. Docs touch: `docs/filament-admin-layout-design.md` §1/§9, `docs/testing-coverage.md`, CLAUDE.md test count line.

Everything tagged [NEW-PHASE] (verification queue, vendor PO/bills, delivery/handover tile, Financial Analytics) and [STRIP] (fake integrations, wrong tax copy, VA/gateway, templates, batch approvals, role tabs, decorative chrome, Material icons, color-dot-only identity) must not be built or copied.
